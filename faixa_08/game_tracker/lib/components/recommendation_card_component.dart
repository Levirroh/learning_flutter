import 'package:flutter/material.dart';

class RecommendationCardComponent extends StatelessWidget {
  const RecommendationCardComponent({super.key, icon, value, label});

  @override
  Widget build(BuildContext context) {
    return Card(child: Text("RecommendationCardComponent"));
  }
}

import 'package:flutter/material.dart';

class GameCardComponent extends StatelessWidget {
  const GameCardComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(child: Text('GameCardComponent'));
  }
}
